# Loops generate repeated structure. `for i = start, stop do` iterates an
# inclusive range; the loop variable `i` is a typed value in expressions
# and `$i` interpolates into a bare string.
@base_port = 8000

for i = 1, 3 do
    server {
        port = base_port + i
        name = node-$i
    }
end
