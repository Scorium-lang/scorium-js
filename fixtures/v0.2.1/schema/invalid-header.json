{
  "name": "schema rejects a disallowed node header",
  "source": "server secondary {\n}\n",
  "schema": {
    "nodes": { "server": { "allowed_headers": ["primary"] } }
  },
  "expect_errors": [
    { "code": "scorium::schema::invalid_header", "node": "server" }
  ]
}
